version: "3.9"

services:
  db:
    build: .
    container_name: innovatech-db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
    name: innovatech-db-data